請完整實作 `tasks/001-slime-monster/SPEC.md` 描述的史萊姆怪物功能。

工作要求：
1. 先閱讀 `AGENTS.md`（專案規範）與 `tasks/001-slime-monster/SPEC.md`（本任務規格），再閱讀相關既有程式碼（`src/entities/`、`src/scenes/VillageScene.ts`、`src/data/`）
2. 依 SPEC 實作，完成後依 `tasks/001-slime-monster/CHECKLIST.md` 自檢
3. 最後執行 `npx tsc` 與 `npm run build`，兩者必須零錯誤；有錯誤就修到零為止
4. 禁止：修改 `tasks/` 下任何檔案、修改 `src/systems/`、啟動 dev server、安裝新依賴
5. 完成後用條列摘要你改了哪些檔案與做了哪些 SPEC 未明說的決策
