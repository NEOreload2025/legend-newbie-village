請完整實作 `tasks/002-loot-pickup/SPEC.md` 描述的掉寶與拾取功能。

工作要求：
1. 先閱讀 `AGENTS.md`、`tasks/002-loot-pickup/SPEC.md`，再閱讀相關既有程式碼（`src/entities/`、`src/scenes/VillageScene.ts`、`src/ui/Hud.ts`、`src/data/`）
2. 依 SPEC 實作
3. 最後執行 `npx tsc` 與 `npm run build`，必須零錯誤；有錯就修到零
4. 禁止：修改 `tasks/` 下任何檔案、修改 `src/systems/`、啟動 dev server、安裝新依賴
5. 完成後條列摘要：改了哪些檔案、做了哪些 SPEC 未明說的決策
